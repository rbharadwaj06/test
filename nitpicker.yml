branches:
  - name: master
  - name: official/*
  - name: user/rbharadwaj06/
    enabled: false
comments:
  - markdown: "# Header \n This applies to _everything_!"
    pathFilter:
      - "/*.*"
  - markdown: "Don't touch files in this path without talking to us!"
    blocking: true
    pathFilter:
      - /src/**
      - "!/src/client/**"
